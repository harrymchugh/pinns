Bootstrap: docker
From: pytorch/pytorch:latest

%files
    entrypoint.sh /entrypoint.sh

%post
    wget -q -O - https://dl.openfoam.com/add-debian-repo.sh | bash
    apt-get install -y openfoam2212

%startscript
    source /entrypoint.sh